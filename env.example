# ============================================================================
# MP3 Transcriber App - Umgebungsvariablen Referenz
# ============================================================================
# Diese Datei dient als Referenz für alle benötigten Umgebungsvariablen.
#
# LOKAL:   Kopiere nach .env und fülle die Werte aus
# RAILWAY: Setze diese Variablen im Railway Dashboard unter
#          "Service" → "Variables"
#
# ACHTUNG: Niemals echte Credentials in diese Datei eintragen!
# ============================================================================

# -----------------------------------------------------------------------
# Server
# -----------------------------------------------------------------------
NODE_ENV=production
# PORT wird von Railway automatisch gesetzt - NICHT manuell setzen!
# PORT=5000

# -----------------------------------------------------------------------
# PostgreSQL Datenbank
# -----------------------------------------------------------------------
# Railway stellt diese Werte automatisch bereit wenn PostgreSQL Add-on
# hinzugefügt wird. Alternativ kann DATABASE_URL verwendet werden.
POSTGRES_HOST=your-railway-postgres-host.railway.internal
POSTGRES_PORT=5432
POSTGRES_USER=postgres
POSTGRES_PASSWORD=your-secure-password
POSTGRES_DB=railway

# Alternativ: Railway stellt auch DATABASE_URL bereit (alle Infos in einer URL)
# DATABASE_URL=postgresql://postgres:password@host:5432/railway

# -----------------------------------------------------------------------
# Authentifizierung
# -----------------------------------------------------------------------
# JWT Secret: Mindestens 32 Zeichen, zufällig generiert
# Generieren: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=your-very-long-random-secret-here-minimum-32-chars

# -----------------------------------------------------------------------
# RunPod APIs (für Whisper + Llama über Cloud)
# -----------------------------------------------------------------------
# Wird für normale User verwendet (nicht-Admin)
RUNPOD_WHISPER_ENDPOINT=your-runpod-whisper-endpoint-id
RUNPOD_LLAMA_ENDPOINT=your-runpod-llama-endpoint-id
RUNPOD_API_KEY=your-runpod-api-key

# -----------------------------------------------------------------------
# Lokaler KI-Service (Hybrid-Architektur für Admin-User)
# -----------------------------------------------------------------------
# URL des lokalen FastAPI-Services (erreichbar via Cloudflare Tunnel)
# Nur setzen wenn Admin-User lokale Transkription/Summarize nutzen sollen.
# Leer lassen = lokale Transkription deaktiviert (nur RunPod wird verwendet)
LOCAL_TRANSCRIBE_SERVICE_URL=https://your-subdomain.your-domain.de

# API-Key zur Absicherung des lokalen Services (muss mit local-ai-service/.env übereinstimmen)
LOCAL_SERVICE_API_KEY=your-shared-secret-api-key

# -----------------------------------------------------------------------
# Upload-Verzeichnis (optional)
# -----------------------------------------------------------------------
# Railway: Nicht persistent! Uploads werden in PostgreSQL BYTEA gespeichert.
# UPLOAD_DIR=./uploads

# -----------------------------------------------------------------------
# CORS (optional)
# -----------------------------------------------------------------------
# Erlaubte Origins für CORS (leer = alle erlaubt)
# ALLOWED_ORIGIN=https://mp3-transcribe.m4itexpertsgmbh.de
