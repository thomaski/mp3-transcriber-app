# React Development Server Configuration
# Erlaubt Zugriff aus dem lokalen Netzwerk

# Host binding (0.0.0.0 = alle Netzwerk-Interfaces)
HOST=0.0.0.0

# Port (Standard: 3000)
PORT=3000

# Disable Host Check für lokales Netzwerk
DANGEROUSLY_DISABLE_HOST_CHECK=true

# Development-only: Show demo credentials on login page
REACT_APP_SHOW_DEMO_CREDENTIALS=true
